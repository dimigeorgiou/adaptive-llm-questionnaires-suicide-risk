# Placeholders only. Copy to .env (git-ignored) if you prefer environment variables.
# Never commit real values.
ADAPTIVE_QUESTIONNAIRES_CONFIG=./config/config.ini
OPENAI_API_KEY=YOUR_OPENAI_KEY_HERE
# 1 enables logging of clinical free text (local debugging on an authorised machine only)
ADAPTIVE_QUESTIONNAIRES_LOG_CLINICAL_TEXT=0

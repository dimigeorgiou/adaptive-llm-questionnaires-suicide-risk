# Companion code — Adaptive LLM Questionnaires (NICE TEAS Europe 2026)
# Keep in sync with [project.dependencies] in pyproject.toml
# (tests/test_dependencies.py checks this).

## Core
numpy>=1.24
pandas>=2.0
tqdm>=4.60
matplotlib>=3.7
seaborn>=0.12

## Config / framework
PyYAML>=6.0
requests>=2.28
retry>=0.9.2

## Google Sheets
google-api-python-client>=2.0
google-auth>=2.0
google-auth-httplib2>=0.1
google-auth-oauthlib==0.4.1
oauth2client>=4.1.3

## OpenAI
openai>=1.0
tiktoken>=0.5

# Optional extras (not needed by the pipeline) — see pyproject.toml:
#   [dev]        pytest
#   [semantic]   sentence-transformers  (multilingual embedding redundancy backend)
#   [dropbox]    dropbox                (disabled OAuth-token sync helper)
#   [legacy-db]  dataset                (unused MkI.DataSet helper)
#   [gmail]      beautifulsoup4         (unused Gmail helper)
